<?xml version="1.0" encoding="UTF-8"?>
<project name="CadastroAlunosNetBeans-impl" default="default" basedir="..">
 <target name="-init"><property file="nbproject/project.properties"/></target>
 <target name="compile" depends="-init">
  <mkdir dir="${build.classes.dir}"/>
  <javac srcdir="${src.dir}" destdir="${build.classes.dir}" includeantruntime="false" encoding="UTF-8" source="${javac.source}" target="${javac.target}"/>
 </target>
 <target name="jar" depends="compile">
  <mkdir dir="${dist.dir}"/>
  <jar destfile="${dist.jar}" basedir="${build.classes.dir}">
   <manifest><attribute name="Main-Class" value="${main.class}"/></manifest>
  </jar>
 </target>
 <target name="run" depends="compile">
  <java classname="${main.class}" fork="true"><classpath path="${build.classes.dir}"/></java>
 </target>
 <target name="clean" depends="-init"><delete dir="${build.dir}"/><delete dir="${dist.dir}"/></target>
 <target name="default" depends="jar"/>
</project>
